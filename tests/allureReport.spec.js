import{test,expect}from '@playwright/test';

test('Allure Report',async({page})=>{

    // install using this cmd - npm i -D @playwright/test allure-playwright
    // install command line - install -g allure-commandline --save-dev
    // npx playwright test allureReport.spec.js
    // allure generate allure-results -o allure-report -clean
    // allure open allure-report
    // setup the config file for report
    // 

    console.log('Allure Reports')

})